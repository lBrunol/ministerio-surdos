.list-categories {

    margin: 0;

    > .item {
        padding: 0;
        margin: 0 10px 0 0;
        font-size: 12px;
        font-weight: bold;
        text-transform: uppercase;
        display: inline-block;

        &:before {
            content: '•';
            margin-right: 5px;
            font-size: 100%;
            display: inline;
            position: static;
        }
    }
}